# local-image-gen X / Twitter poster kit

Three 1600 × 900 posters derived from the product positioning and provider catalog in `README.zh-CN.md` and `references/providers.md`.

## 01 — Login first

Suggested post:

> 已经登录 Grok、Antigravity、Cursor 或 Codex？`local-image-gen` 把你已有的登录变成统一的生图 / 改图 CLI 和 portable skill。官方 API Key 只是显式兜底，不是前提。Codex 供应方为实验通道。  
> https://github.com/DandreYang/local-image-gen

Alt text: 黑色终端空间中的一束橙色命令光流向暖色山景，左侧写有“用已登录的编程助手直接生图、改图”。

## 02 — One command, many models

Suggested post:

> 一条命令，多种图像模型。`local-image-gen` 统一路由 Grok Imagine、Nano Banana 与 GPT Image；本地订阅优先，官方 API 显式兜底。模型与参数映射都写在仓库里。  
> https://github.com/DandreYang/local-image-gen

Alt text: 一条橙色输入线经过本地路由核心，分发成山景、水彩狐狸、产品静物与抽象艺术四种图像。

## 03 — Prompt to file

Suggested post:

> 你的登录，直接开画。`local-image-gen` 是 Python 3.9+、仅标准库的本地编排 CLI，也能作为多种编程助手的 portable skill。使用自有登录或自有官方 Key；生成会消耗所选后端配额或账单。  
> https://github.com/DandreYang/local-image-gen

Alt text: 一条终端命令经过黑色本地编排模块，最终生成一张装裱好的山景图。

## Sources

- `*.svg`: editable 1600 × 900 poster source
- `*.png`: X / Twitter-ready export
- `backgrounds/*.png`: text-free image-generation outputs
- `PROMPTS.md`: final background prompt set
